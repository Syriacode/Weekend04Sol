package example.org.test.weekend04sol;

public class Encode {
    public String encode (String string){
        StringBuilder stringBuilder = new StringBuilder();
        char tmp = string.charAt(0);
        int count = 1;
        for (int i=0; i<string.length();i++){
            char curr = string.charAt(i);
            //if (char[i]=="a")
        }
    }
}
